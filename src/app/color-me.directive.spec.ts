import { ColorMeDirective } from './color-me.directive';

describe('ColorMeDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorMeDirective();
    expect(directive).toBeTruthy();
  });
});
